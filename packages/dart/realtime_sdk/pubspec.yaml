name: realtime_sdk
description: Firestore projection readers for the realtime client layer.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  cloud_firestore: ^6.0.0
