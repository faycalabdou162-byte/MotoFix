apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: urban-mobility
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/motofix/auth-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: mobility-platform-config
          env:
            - name: HTTP_ADDR
              value: ":8080"
            - name: FIREBASE_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: firebase-config
                  key: project_id
            - name: INTERNAL_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: internal-jwt
                  key: secret
            - name: FIREBASE_ADMIN_CREDENTIALS_B64
              valueFrom:
                secretKeyRef:
                  name: firebase-admin
                  key: credentials_b64
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: urban-mobility
spec:
  selector:
    app: auth-service
  ports:
    - port: 80
      targetPort: 8080

