apiVersion: apps/v1
kind: Deployment
metadata:
  name: dispatch-service
  namespace: urban-mobility
spec:
  replicas: 5
  selector:
    matchLabels:
      app: dispatch-service
  template:
    metadata:
      labels:
        app: dispatch-service
    spec:
      containers:
        - name: dispatch-service
          image: ghcr.io/motofix/dispatch-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: mobility-platform-config
          env:
            - name: HTTP_ADDR
              value: ":8080"
---
apiVersion: v1
kind: Service
metadata:
  name: dispatch-service
  namespace: urban-mobility
spec:
  selector:
    app: dispatch-service
  ports:
    - port: 80
      targetPort: 8080

