apiVersion: apps/v1
kind: Deployment
metadata:
  name: driver-service
  namespace: urban-mobility
spec:
  replicas: 3
  selector:
    matchLabels:
      app: driver-service
  template:
    metadata:
      labels:
        app: driver-service
    spec:
      containers:
        - name: driver-service
          image: ghcr.io/motofix/driver-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: mobility-platform-config
          env:
            - name: HTTP_ADDR
              value: ":8080"
            - name: INTERNAL_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: internal-jwt
                  key: secret
            - name: FIREBASE_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: firebase-config
                  key: project_id
            - name: FIREBASE_ADMIN_CREDENTIALS_B64
              valueFrom:
                secretKeyRef:
                  name: firebase-admin
                  key: credentials_b64
---
apiVersion: v1
kind: Service
metadata:
  name: driver-service
  namespace: urban-mobility
spec:
  selector:
    app: driver-service
  ports:
    - port: 80
      targetPort: 8080
