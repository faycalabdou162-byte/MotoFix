apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: urban-mobility
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/motofix/payment-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: mobility-platform-config
          env:
            - name: HTTP_ADDR
              value: ":8080"
            - name: INTERNAL_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: internal-jwt
                  key: secret
            - name: FIREBASE_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: firebase-config
                  key: project_id
            - name: FIREBASE_ADMIN_CREDENTIALS_B64
              valueFrom:
                secretKeyRef:
                  name: firebase-admin
                  key: credentials_b64
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: urban-mobility
spec:
  selector:
    app: payment-service
  ports:
    - port: 80
      targetPort: 8080
